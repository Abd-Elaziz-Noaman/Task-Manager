import styled from "styled-components";

export const MainContainer = styled.div`
  padding: 0 1rem;
  /* padding-right: ${({ open }) => (open ? "1rem" : "3rem")}; */
  border-left: 1px solid rgba(0, 0, 0, 0.12);
`;
